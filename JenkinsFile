pipeline{
  agent any 
    stages {
      stage('checkout'){
      step{
        checkout scm
      }
      }
      
      stage('build'){
      step{
        sh mvn build
        }
        step
        {
        sh flyway baseline 
        sh flyway migrate
        sh flyway validate 
        sh flyway info
        }
      }
    }
}
