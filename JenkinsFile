pipeline{
  agent any 
    stages {
      stage('clone'){
      steps {
                checkout scm
            }
      }
      
      stage('build'){
      steps{
           sh 'mvn "flyway:migrate" "flyway:validate" "flyway:info"'  
        }
      }
      }
}
