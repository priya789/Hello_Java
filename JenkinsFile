pipeline{
  agent any 
    stages {
      stage('checkout'){
      steps {
                checkout scm
            }
      }
      
      stage('build'){
      steps{
           sh 'mvn "flyway:repair" "flyway:migrate" "flyway:validate" "flyway:info"'  
        }
      }
      }
}
