pipeline{
  agent any 
    stages {
      stage('checkout'){
        checkout scm
      }
      
      stage('build'){
        sh mvn --build
        sh flyway baseline 
        sh flyway migrate
        sh flyway validate 
        sh flyway info
      }
    }
}
